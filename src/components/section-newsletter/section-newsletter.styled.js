import { themeGet } from '@styled-system/theme-get';
import styled from 'styled-components';

export const SectionNewsletterStyled = styled.section`
  background-color: ${themeGet('colors.black')};
  padding: 8.5rem 1rem;
  text-align: center;
`;
