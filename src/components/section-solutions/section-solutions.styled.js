import { themeGet } from '@styled-system/theme-get';
import styled from 'styled-components';

export const SectionSolutionsStyled = styled.section`
  background-color: ${themeGet('colors.white')};
  padding-bottom: 3.5rem;
  padding-top: 3.5rem;
`;
